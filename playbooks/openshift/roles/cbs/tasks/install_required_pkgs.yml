---
# Set a var with packages to install as below
# which is what is currently in defaults/main.yml
- name: update all packages
  yum: name=* state=latest

- name: Install packages on repo_host in group_vars
  yum:
    name: "{{ item }}"
    state: present
  with_items:
    - "centos_packager"