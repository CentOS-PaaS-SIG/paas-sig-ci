# this inventory is a 3 node cluster.
# 1 master, 2 nodes, very simple.

[masters]
{{ duffy_metadata.hosts[0] }}

[nodes]
{{ duffy_metadata.hosts[1] }}
{{ duffy_metadata.hosts[2] }}

[repo_host]
{{ duffy_metadata.hosts[1] }}

